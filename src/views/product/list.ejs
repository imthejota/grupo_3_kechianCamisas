<!DOCTYPE html>
<html lang="en">


    <%- include('../partials/head.ejs', {estilo: 'modificar', titulo: 'Lista'})  %>


<body>
    <%- include ('../partials/header.ejs') %>
    
    <main class="noventa listaProducto">
    <% if (products.length > 0) { %>
        <ul>
            <% products.forEach(element => { %>
                <%- include('../partials/product.ejs',{element:element})  %>
                    <% }) %>
        </ul>
        <% } else { %>
            <h2>
                No hay productos <a href="/productos">Volver</a>
            </h2>
            <% } %>

                <nav>
                    <a href="/products/crear">Agregar un Nuevo Producto</a>
                </nav>
    </main>
    <%- include('../partials/footer.ejs', {estilo: 'footer', titulo: 'Lista'})  %>
</body>
    
</html>