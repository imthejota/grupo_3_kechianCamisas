<!DOCTYPE html>
<html lang="en">

<%- include ('../partials/head.ejs', {estilo: 'perfilUsuario', titulo: 'Perfil de Usuario'}) %>

<body>
    <%- include ('../partials/header.ejs') %>
    <main class="mainPerfilUsuario">
        <section class="contenedorPerfilUsuario">
            
            <picture class="contenedorImagenUsuario">
                <img class="imagenUsuario" src="/users/<%=user.image%>" alt=""> 
            </picture> 
            
            <section class="fichaPerfilUsuario">
                <div class="contenedorficha">
                <div class="tituloPerfilUsuario">
                    <p class="perfilUsuario">MI PERFIL</p>
                </div>

                <div class="titulo"> 
                    <p class="usuario">Nombre:</p>
                    <h1 class="datoUsuario"><%= user.firstName%></h1>
                </div>

                <div class="titulo">
                    <p class="usuario">Apellido:</p>
                    <h1 class="datoUsuario"><%= user.lastName%></h1>
                </div>

                <div class="titulo">
                    <p class="usuario">Ubicación:</p>
                    <h1 class="datoUsuario"><%= user.location%></h1>
                </div>

                <div class="titulo">
                    <p class="usuario">Email:</p>
                    <h1 class="datoUsuario"><%= user.email%></h1>
                </div>
            </div>
                <div id="botonConfig" class="botonConfig">
                <button class="perfilConfig"><a class="aPerfilConfig" href="/user/update/<%= user.id %>">Editar perfil</a></button>
                </div>   
            </section>
            
                        
            
            
 
        </section>
    </main>         
    
    <%- include ('../partials/footer.ejs') %>
</body>